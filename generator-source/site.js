document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth'});}}));
document.querySelectorAll('.nav-menu').forEach(menu=>menu.addEventListener('toggle',()=>{if(menu.open)document.querySelectorAll('.nav-menu').forEach(other=>{if(other!==menu)other.open=false})}));
document.querySelectorAll('[data-calendar]').forEach(calendar=>{const grid=calendar.querySelector('[data-calendar-grid]'),month=calendar.querySelector('[data-calendar-month]'),input=calendar.querySelector('[data-calendar-input]'),today=new Date();today.setHours(0,0,0,0);let cursor=new Date(today.getFullYear(),today.getMonth(),1);const iso=date=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;const draw=()=>{month.textContent=cursor.toLocaleDateString('en-US',{month:'long',year:'numeric'});grid.innerHTML='';for(let i=0;i<cursor.getDay();i++){const blank=document.createElement('span');blank.className='calendar-blank';grid.append(blank)}const days=new Date(cursor.getFullYear(),cursor.getMonth()+1,0).getDate();for(let day=1;day<=days;day++){const date=new Date(cursor.getFullYear(),cursor.getMonth(),day),button=document.createElement('button');button.type='button';button.className='calendar-date';button.textContent=day;button.setAttribute('aria-label',date.toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric',year:'numeric'}));if(date<today)button.disabled=true;if(input.value===iso(date))button.classList.add('is-selected');button.addEventListener('click',()=>{input.value=iso(date);draw()});grid.append(button)}};calendar.querySelector('[data-calendar-prev]').addEventListener('click',()=>{cursor.setMonth(cursor.getMonth()-1);if(cursor<new Date(today.getFullYear(),today.getMonth(),1))cursor=new Date(today.getFullYear(),today.getMonth(),1);draw()});calendar.querySelector('[data-calendar-next]').addEventListener('click',()=>{cursor.setMonth(cursor.getMonth()+1);draw()});draw()});
document.querySelectorAll('[data-time-slots]').forEach(group=>{const input=group.querySelector('[data-time-input]');group.querySelectorAll('button[data-time]').forEach(button=>button.addEventListener('click',()=>{input.value=button.dataset.time;group.querySelectorAll('button').forEach(item=>item.classList.toggle('is-selected',item===button))}))});
document.querySelectorAll('[data-calendar]').forEach(calendar=>calendar.addEventListener('click',event=>{if(!event.target.classList.contains('calendar-date')||event.target.disabled)return;const form=calendar.closest('form'),time=form.querySelector('[data-time-slots]'),details=form.querySelector('[data-schedule-details]');if(time){time.hidden=false;time.querySelector('[data-time-input]').value='';time.querySelectorAll('.is-selected').forEach(item=>item.classList.remove('is-selected'))}if(details)details.hidden=true;}));
document.querySelectorAll('[data-time-slots]').forEach(group=>group.addEventListener('click',event=>{if(!event.target.matches('button[data-time]'))return;const details=group.closest('form').querySelector('[data-schedule-details]');if(details){details.hidden=false;setTimeout(()=>details.querySelector('select, input, textarea')?.focus(),0)}}));
document.querySelectorAll('[data-quote-trigger]').forEach(trigger=>trigger.addEventListener('click',event=>{event.preventDefault();const modal=document.querySelector('[data-quote-modal]');modal.hidden=false;setTimeout(()=>modal.querySelector('input')?.focus(),0)}));
document.querySelectorAll('[data-quote-close]').forEach(control=>control.addEventListener('click',()=>document.querySelector('[data-quote-modal]').hidden=true));
document.querySelectorAll('[data-quote-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const response=form.querySelector('.form-response');response.textContent='Thanks. Your quote request is ready for the Petssible team to review.';form.reset();}));
document.querySelectorAll('[data-booking-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const response=form.querySelector('.form-response'),date=form.querySelector('[data-calendar-input]'),time=form.querySelector('[data-time-input]');if((date&&!date.value)||(time&&!time.value)){response.textContent='Please choose a preferred date and time window before sending your request.';return}response.textContent='Thanks. Your appointment request is ready for the Petssible team to review.';form.reset();form.querySelectorAll('.is-selected').forEach(item=>item.classList.remove('is-selected'));}));
